# Considere que a cobertura da tinta é de 1 litro para cada 3m² e que a tinta
# é vendida em latas de 18 litros, que custam R$ 80,00. Crie uma função que
# retorne dois valores em uma tupla contendo a quantidade de latas de tinta a
# serem compradas e o preço total a partir do tamanho de uma parede(em m²).
import math


def tinta(area):
    preco = 80
    tamanho_area = area / 3
    latas_unid = tamanho_area // 18
    if tamanho_area % 18:
        latas_unid += 1
    return latas_unid, latas_unid * preco


print(tinta(100))  # (2.0, 160.0)

# ou utilizando o módulo 'math'


def tinta(area):
    preco = 80
    tamanho_area = area / 3
    latas_unid = math.ceil(tamanho_area / 18)
    return latas_unid, latas_unid * preco


print(tinta(20))  # (1, 80)
