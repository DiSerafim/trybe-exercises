# Imagine que você recebe dois arrays de tamanho igual, array1 e array2.
# Para cada elemento do array1, realize uma busca binária no array2.
# Mostre que a ordem de complexidade do algoritmo resultante é O(n * log n), ou O(n log n).
""" R = """
# Se a cada busca  binária tem uma complexidade 0(log n) e fazemos "n" buscas, sendo cada uma nessa complexidade..
# a complexidade final é "0(n * log n)"