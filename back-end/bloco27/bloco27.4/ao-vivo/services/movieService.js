const MoviesModel = require('../models/movieModel');

const isValid = (title, directedBY, releaseYear) => {

};
const create = sync ({ title, directedBY, releaseYear }) => {
  const isMovieValid = 
};
